# 🎨 Trailclass UI 视觉规范系统 (Design System) v1.0

**核心理念：** 活力 (Energetic)、专业 (Professional)、成长 (Growth)、亲和力 (Friendly)。
**适用范围：** 老师端、学生报告端、管理员后台。

---

## 1. 色彩体系 (Color Palette)

采用了高饱和度的蓝黄撞色，体现青少儿体育教育的活力。

### 品牌主色 (Primary Colors)

* **Trailclass Blue (天空蓝)**
* **色值:** `#26B7FF`
* **用途:** 主按钮背景、进度条、关键文字高亮、雷达图填充。
* **Tailwind:** `bg-[#26B7FF]`, `text-[#26B7FF]`


* **Energy Yellow (活力黄)**
* **色值:** `#FDE700`
* **用途:** 强调背景（如雷达图卡片）、次级按钮、头像背景、装饰性图标。
* **Tailwind:** `bg-[#FDE700]`, `text-[#FDE700]`



### 中性色 (Neutrals)

* **Dark Black (深黑)**
* **色值:** `#333333`
* **用途:** 主标题、正文内容、强调按钮背景。


* **Soft Gray (柔灰)**
* **色值:** `#9CA3AF` (Text) / `#F6F6F6` (Background)
* **用途:** 辅助文字、页面底色、未选中状态。


* **Pure White (纯白)**
* **色值:** `#FFFFFF`
* **用途:** 卡片背景、文字反白。



### 语义色 (Semantic) - *管理员后台专用*

* **Success Green:** `#10B981` (用于“已完成”、“提交成功”)
* **Error Red:** `#EF4444` (用于“删除”、“异常状态”)

---

## 2. 排版系统 (Typography)

主要使用无衬线字体 (Sans-serif) 保持现代感，证书部分使用衬线字体 (Serif) 增加仪式感。

### 字体家族 (Font Family)

* **Primary:** 系统默认 Sans-serif (Inter, SF Pro, Roboto 等)。
* **Secondary (证书专用):** Serif (Times New Roman, Georgia 等)，用于 "Certificate" 字样。

### 字号与字重 (Scale & Weight)

* **Display (超大标题):** `text-5xl` to `text-7xl` | **Black (900)** | 用于学生姓名、分数。
* **Heading 1 (页面标题):** `text-3xl` | **Bold (700)** | 用于 "Trial Session Report"。
* **Heading 2 (模块标题):** `text-2xl` | **Bold (700)** | 用于 "Performance Stats"。
* **Heading 3 (小标题):** `text-lg` | **ExtraBold (800)** | 用于题目、图表标签。
* **Body (正文):** `text-base` | **Medium (500)** | 用于评语、说明文案。
* **Caption (标签/辅助):** `text-xs` | **Bold (700)** | 大写 (Uppercase)，加大字间距 (`tracking-widest`)。

---

## 3. 组件样式规范 (Components)

### 圆角 (Border Radius)

Trailclass 的风格非常圆润，极少出现直角。

* **大卡片/容器:** `32px` (`rounded-[32px]`) 或 `24px` (`rounded-3xl`)。
* **按钮:** `Full` (`rounded-full`) 或 `12px` (`rounded-xl`)。
* **内部元素:** `16px` (`rounded-2xl`)。

### 阴影 (Shadows)

使用柔和的弥散阴影，营造悬浮感，避免生硬的黑影。

* **Card Shadow:** `shadow-sm` (日常卡片)。
* **Float Shadow:** `shadow-lg` (弹窗、悬浮按钮)。
* **Glow Effect:** 活力黄元素常伴随发光效果 `shadow-[0_0_25px_rgba(253,231,0,0.7)]`。

### 按钮 (Buttons)

1. **Primary Button (主操作):**
* 背景: `#FDE700` (黄) 或 `#333333` (黑)
* 文字: 对应反色
* 形状: 全圆角
* 交互: Hover 时略微放大 (`scale-105`) + 阴影加深。


2. **Secondary Button (次操作):**
* 背景: `bg-gray-100` 或 `bg-blue-50`
* 文字: 灰色或蓝色
* 形状: 全圆角



### 进度条 (Progress Bars)

* **槽 (Track):** `bg-gray-100` 或 `bg-white/20` (在深色背景下)。
* **条 (Indicator):** `#26B7FF` (蓝)，圆角处理。

---

## 4. 布局与间距 (Layout & Spacing)

### 移动端优先 (Mobile First)

* 页面两侧保留 `px-6` (24px) 的安全边距。
* 模块之间垂直间距 `space-y-6` 或 `space-y-8`。

### 装饰元素 (Decorations)

* **Winding Path (蜿蜒路径):** 虚线描边，浅蓝色，贯穿页面，象征成长轨迹。
* **Icons:** 使用 `Lucide-react` 图标库，通常为 20px-24px，线条风格。
* **Avatars:** 使用 `Dicebear` 生成的扁平化矢量头像，带有鲜艳的黄色/蓝色背景。

---

## 5. 给开发者的 Tailwind 配置建议

如果你想在项目中快速应用这套规范，建议将 `tailwind.config.ts` 更新如下：

```typescript
import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    "./pages/**/*.{js,ts,jsx,tsx,mdx}",
    "./components/**/*.{js,ts,jsx,tsx,mdx}",
    "./app/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    extend: {
      colors: {
        brand: {
          blue: "#26B7FF",    // 主色蓝
          yellow: "#FDE700",  // 主色黄
          black: "#333333",   // 主色黑
        },
      },
      borderRadius: {
        '4xl': '32px', // 用于大卡片
      },
      fontFamily: {
        sans: ['var(--font-inter)', 'sans-serif'],
        serif: ['var(--font-playfair)', 'serif'], // 假设你引入了 Playfair Display 作为证书字体
      }
    },
  },
  plugins: [require("tailwindcss-animate")], // 之前用到的动画插件
};
export default config;

```

**后续开发管理员后台时，请严格遵守上述规范（特别是圆角、配色和间距），以保证产品体验的一致性。**